import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
  // Add global styles that can be used throughout the app
});

export default globalStyles;

// This file can contain global styles that are used across multiple components.
// For example, common colors, fonts, or layout styles.
